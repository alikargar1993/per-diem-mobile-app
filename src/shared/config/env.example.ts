/**
 * API configuration template.
 * Copy to env.ts and fill in values before running the app:
 *
 *   cp src/shared/config/env.example.ts src/shared/config/env.ts
 */
export const API_BASE_URL = 'http://localhost:3001';

/** Must match API_GENERAL_TOKEN in per-diem-backend/.env (min 16 characters). */
export const API_GENERAL_TOKEN = '';

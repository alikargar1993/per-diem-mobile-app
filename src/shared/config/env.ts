/**
 * API and app configuration.
 * Update these values to match your local backend (see .env.example).
 */
export const API_BASE_URL = 'http://localhost:3001';

/** Bearer / X-Api-Token value for GET endpoints (API_GENERAL_TOKEN on the server). */
export const API_GENERAL_TOKEN = '';

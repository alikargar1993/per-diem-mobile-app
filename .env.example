# API_BASE_URL=http://localhost:3001
# API_GENERAL_TOKEN=your_general_token_min_16_chars

# For physical devices, use your machine LAN IP instead of localhost:
# API_BASE_URL=http://192.168.1.100:3001

# Copy values into src/shared/config/env.ts (or wire react-native-config later).

# Mobile config lives in src/shared/config/env.ts (gitignored).
# Copy the template and set your backend URL + token:
#
#   cp src/shared/config/env.example.ts src/shared/config/env.ts
#
# For physical devices, use your machine LAN IP instead of localhost in API_BASE_URL.
